@model IEnumerable<WebApp.Models.Unit>
@using WebApp.Areas.Administration.Services
@{
    ViewBag.Title = ResourceProvider.GetResource("Units");
}
<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">@ResourceProvider.GetResource("Home")</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading"><span class="glyphicon glyphicon-link"></span> @ResourceProvider.GetResource("Units")</div>
        <table class="table">
            <tr>
                <th>@ResourceProvider.GetResource("Unit")</th>
                <th>@ResourceProvider.GetResource("Action")</th>
            </tr>
            @foreach (var unit in Model)
            {
                <tr>
                    <td>@unit.Name</td>
                    <td>
                        <a asp-area="Administration" asp-controller="Units" asp-action="Details" asp-route-id="@unit.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                        <a asp-area="Administration" asp-controller="Units" asp-action="Edit" asp-route-id="@unit.Id"><span class="glyphicon glyphicon-pencil"></span></a> |
                        <a asp-area="Administration" asp-controller="Units" asp-action="Delete" asp-route-id="@unit.Id"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="form-group">
        <a class="btn btn-primary btn-lg btn-block" asp-controller="Units" asp-area="Administration" asp-action="Create" role="button">@ResourceProvider.GetResource("Add")</a>
    </div>
</div>
