@model WebApp.Models.Catalog
@using WebApp.Areas.Administration.Services
@{
    ViewBag.Title = ResourceProvider.GetResource("Catalog");
}
<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">@ResourceProvider.GetResource("Home")</a></li>
    <li><a asp-area="Administration" asp-action="Index" asp-controller="Catalogs">@ResourceProvider.GetResource("Catalogs")</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading"><span class="glyphicon glyphicon-eye-open"></span> @ResourceProvider.GetResource("ExtendedInfo")</div>
        <table class="table">
            <tbody>
                <tr>
                    <th scope="row">@ResourceProvider.GetResource("Identifier")</th>
                    <td>@Model.Id</td>
                </tr>
                <tr>
                    <th scope="row">@ResourceProvider.GetResource("Catalog")</th>
                    <td>@Model.Name</td>
                </tr>
                <tr>
                    <th scope="row">@ResourceProvider.GetResource("SubSection")</th>
                    <td>@Model.SubSection.Name</td>
                </tr>
            </tbody>
        </table>
    </div>
    <div class="form-group">
        <a asp-action="Edit" class="btn btn-primary btn-lg btn-block" asp-route-id="@Model.Id">@ResourceProvider.GetResource("Edit")</a>
    </div>
    <a asp-area="Administration" asp-action="Index" asp-controller="Catalogs" class="btn btn-primary btn-lg btn-block" role="button">@ResourceProvider.GetResource("Catalogs")</a>
</div>