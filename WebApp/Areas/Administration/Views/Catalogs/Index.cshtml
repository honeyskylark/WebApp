@model IEnumerable<WebApp.Models.Catalog>
@using WebApp.Areas.Administration.Services
@{
    ViewBag.Title = ResourceProvider.GetResource("Catalogs");
}
<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">@ResourceProvider.GetResource("Home")</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading"><span class="glyphicon glyphicon-link"></span> @ResourceProvider.GetResource("DataBase")</div>
        <table class="table">
            <tr>
                <th>@ResourceProvider.GetResource("Catalog")</th>
                <th>@ResourceProvider.GetResource("SubSection")</th>
                <th>@ResourceProvider.GetResource("Actions")</th>
            </tr>
            @foreach (var catalog in Model)
            {
                <tr>
                    <td>@catalog.Name</td>
                    <td>@catalog.SubSection.Name</td>
                    <td>
                        <a asp-controller="Catalogs" asp-area="Administration" asp-action="Details" asp-route-id="@catalog.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                        <a asp-controller="Catalogs" asp-area="Administration" asp-action="Edit" asp-route-id="@catalog.Id"><span class="glyphicon glyphicon-pencil"></span></a> |
                        <a asp-controller="Catalogs" asp-area="Administration" asp-action="Delete" asp-route-id="@catalog.Id"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="form-group">
        <a class="btn btn-primary btn-lg btn-block" asp-controller="Catalogs" asp-area="Administration" asp-action="Create" role="button">@ResourceProvider.GetResource("Add")</a>
    </div>
</div>  