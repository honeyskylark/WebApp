@model IEnumerable<WebApp.Models.From>
@using WebApp.Services
@{
    ViewBag.Title = ResourceProvider.GetResource("Froms");
}
<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">@ResourceProvider.GetResource("Home")</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading"><span class="glyphicon glyphicon-link"></span> @ResourceProvider.GetResource("Froms")</div>
        <table class="table">
            <tr>
                <th>@ResourceProvider.GetResource("From")</th>
                <th>@ResourceProvider.GetResource("Actions")</th>
            </tr>
            @foreach (var from in Model)
            {
                <tr>
                    <td>@from.Name</td>
                    <td>
                        <a asp-area="Administration" asp-controller="Froms" asp-action="Details" asp-route-id="@from.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                        <a asp-area="Administration" asp-controller="Froms" asp-action="Edit" asp-route-id="@from.Id"><span class="glyphicon glyphicon-pencil"></span></a> |
                        <a asp-area="Administration" asp-controller="Froms" asp-action="Delete" asp-route-id="@from.Id"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="form-group">
        <a class="btn btn-primary btn-lg btn-block" asp-controller="Froms" asp-area="Administration" asp-action="Create" role="button">@ResourceProvider.GetResource("Add")</a>
    </div>
</div>
