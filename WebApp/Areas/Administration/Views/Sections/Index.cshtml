@model IEnumerable<WebApp.Models.Section>
@using WebApp.Areas.Administration.Services
@{
    ViewBag.Title = ResourceProvider.GetResource("Sections");
}
<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">@ResourceProvider.GetResource("Home")</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading"><span class="glyphicon glyphicon-link"></span> @ViewBag.Title</div>
        <table class="table">
            <tr>
                <th>@ResourceProvider.GetResource("Section")</th>
                <th>@ResourceProvider.GetResource("Actions")</th>
            </tr>

            @foreach (var sec in Model)
            {
                <tr>
                    <td>@sec.Name</td>
                    <td>
                        <a asp-area="Administration" asp-controller="Sections" asp-action="Details" asp-route-id="@sec.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                        <a asp-area="Administration" asp-controller="Sections" asp-action="Edit" asp-route-id="@sec.Id"><span class="glyphicon glyphicon-pencil"></span></a> |
                        <a asp-area="Administration" asp-controller="Sections" asp-action="Delete" asp-route-id="@sec.Id"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="form-group">
        <a class="btn btn-primary btn-lg btn-block" asp-controller="Sections" asp-area="Administration" asp-action="Create" role="button">@ResourceProvider.GetResource("Add")</a>
    </div>
</div>
   