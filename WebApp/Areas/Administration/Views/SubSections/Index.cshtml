@model IEnumerable<WebApp.Models.SubSection>
@using WebApp.Areas.Administration.Services
@{
    ViewBag.Title = ResourceProvider.GetResource("SubSections");
}
<ol class="breadcrumb">
    <li><a asp-area="" asp-action="Index" asp-controller="Home">@ResourceProvider.GetResource("Home")</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="container-fluid">
    <div class="panel panel-primary">
        <div class="panel-heading"><span class="glyphicon glyphicon-link"></span> @ViewBag.Title</div>
        <table class="table">
            <tr>
                <th>@ResourceProvider.GetResource("SubSection")</th>
                <th>@ResourceProvider.GetResource("Section")</th>
                <th>@ResourceProvider.GetResource("Action")</th>
            </tr>

            @foreach (var subsec in Model)
            {
                <tr>
                    <td>@subsec.Name</td>
                    <td>@subsec.Section.Name</td>
                    <td>
                        <a asp-area="Administration" asp-controller="SubSections" asp-action="Details" asp-route-id="@subsec.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                        <a asp-area="Administration" asp-controller="SubSections" asp-action="Edit" asp-route-id="@subsec.Id"><span class="glyphicon glyphicon-pencil"></span></a> |
                        <a asp-area="Administration" asp-controller="SubSections" asp-action="Delete" asp-route-id="@subsec.Id"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="form-group">
        <a class="btn btn-primary btn-lg btn-block" asp-controller="SubSections" asp-area="Administration" asp-action="Create" role="button">@ResourceProvider.GetResource("Add")</a>
    </div>
</div>
