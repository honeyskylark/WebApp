@model IEnumerable<WebApp.Models.Currency>

@{
    ViewData["Title"] = "Валюты";
}
<ol class="breadcrumb">
    <li><a asp-action="Index" asp-controller="Home">Главная</a></li>
    <li class="active">@ViewBag.Title</li>
</ol>
<div class="form-group">
    <a class="btn btn-primary btn-lg btn-block" asp-action="Create" role="button">Добавить Валюту</a>
</div>

<div class="panel panel-primary">
    <div class="panel-heading"><span class="glyphicon glyphicon-link"></span> Валюты</div>
    <table class="table">
        <tr>
            <th>Валюта</th>
            <th>Действия</th>
        </tr>

        @foreach (var currency in Model)
        {
            <tr>
                <td>@currency.Name</td>
                <td>
                    <a asp-action="Details" asp-route-id="@currency.Id"><span class="glyphicon glyphicon-eye-open"></span></a> |
                    <a asp-action="Edit" asp-route-id="@currency.Id"><span class="glyphicon glyphicon-pencil"></span></a> |
                    <a asp-action="Delete" asp-route-id="@currency.Id"><span class="glyphicon glyphicon-trash"></span></a>
                </td>
            </tr>
        }
    </table>
</div>